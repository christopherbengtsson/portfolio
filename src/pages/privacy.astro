---
import PrivacyPage from '../container/astro/PrivacyPage.astro';
import { getEntry } from 'astro:content';

const home = await getEntry('web', 'en/home');
if (!home) throw new Error('Missing en privacy-page content');
---
<PrivacyPage locale="en" content={home.data} />
