---
import LandingPage from '../../container/astro/LandingPage.astro';
import { getEntry } from 'astro:content';

const home = await getEntry('web', 'sv/home');
if (!home) throw new Error('Missing Swedish landing-page content');
---
<LandingPage locale="sv" content={home.data} />
